# MangoWC Border Colors — Parchment Theme

**Date:** 2026-05-31  
**File:** `home.nix` → `xdg.configFile."mango/config.conf".text`

## Summary

Update the two MangoWC border color values to match the parchment palette established by the waybar medieval theme.

## Changes

| Setting | Old value | New value | Color |
|---|---|---|---|
| `focuscolor` | `0xffffffff` (white) | `0xc8a96eff` | Warm amber parchment — matches wallpaper mid-tone |
| `bordercolor` | `0x262625ff` (near-black) | `0x7a5030ff` | Dark oak — same as waybar border |

All other MangoWC settings (`borderpx`, `border_radius`, shadow colors, etc.) are unchanged.
